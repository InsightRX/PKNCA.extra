% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_nca.R
\name{run_nca}
\alias{run_nca}
\title{Run a non-compartmental analysis}
\usage{
run_nca(
  data,
  dose_data = NULL,
  specification = "default",
  dictionary = NULL,
  settings = NULL,
  blq = "<",
  groups = NULL,
  include_cols = NULL,
  intervals = data.frame(start = 0, end = Inf),
  partial_auc = NULL,
  exclude_points = NULL,
  exclude_subjects = NULL,
  exclude_lambda_z = NULL,
  include_lambda_z = NULL,
  units = NULL,
  conversions = NULL,
  sequence_from = NULL,
  add_auctau = TRUE,
  post = list(accumulation = list(parameters = c("auctau", "cmax"))),
  format = c("wide", "long"),
  no_dots = TRUE,
  path = NULL,
  check_grouping = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{data}{dataset in pre-parse format, preferrably created using
\code{create_nca_data()}. Can also be a NONMEM-style dataset with EVID=0|1 column
indicating PK an dosing data.}

\item{dose_data}{optional, a data.frame with all dosing info. Useful for
multi-dose NCAs. Alternative to specifying a NONMEM-style datasett to \code{data}.}

\item{specification}{use specific options for the NCA and for mapping of
input / output data.}

\item{dictionary}{abridged data dictionary for the input dataset, specified
as list object. Requires at least entries for \code{subject_id}, \code{time}, \code{conc},
\code{dose}. Optional entries are: \code{visit}.
If required entries are specified only partially, then default values
(i.e. CDISC) nomenclature will be imputed for missing identifiers.}

\item{settings}{list of settings for NCA. Provided setting names can either
be settings recognized directly by \code{pknca}, or settings that are included in
the map provided by \code{nca_settings_map()} (which are then mapped to \code{pknca}
setting names). In addition to standard PKNCA settings, the following
settings are handled internally and not passed to PKNCA:
\itemize{
\item \code{min.hl.time} (or \code{minHalfLifeTime}): minimum time required for a data
point to be eligible for the lambda-z (terminal slope) calculation. Any
data points with time < this value are excluded from the lambda-z fit
(but remain in the analysis for other parameters such as Cmax and AUClast).
This is implemented by setting \code{exclude_lambda_z} flags internally.
}}

\item{blq}{optional, string to match in concentration data, e.g. "<" or
"BLQ>. If \code{conc} column in the data is \code{character} class, then if \code{blq} is
not NULL, it will detect any values that match this string and set those
values to zero. If NULL, or \code{conc} data is numeric, the data will be used
as-is.}

\item{groups}{optional grouping variable, e.g. \code{ACTARM}.}

\item{include_cols}{Optional. Character vector of column names in \code{data} that
should be merged in with NCA results (including only a single value for each
row). Merge will happen by subject ID, any supplied groupings, and
\code{nca_start} time.}

\item{intervals}{specify time intervals for NCA calculations. The passed argument
should be a data.frame, with at least the \code{start} and \code{end} time for each
row that specifies the interval. \code{Inf} is allowed to specify the end of the
interval. Example:

\if{html}{\out{<div class="sourceCode">}}\preformatted{intervals = bind_rows(
   c(start = 0, end = 24), 
   c(start = 120, end = Inf)
)
}\if{html}{\out{</div>}}

The \code{run_nca()} function will compute all parameters that are specified in
\code{settings()} or in the default settings.}

\item{partial_auc}{a data.frame containing at least the \code{start} and \code{end}
time for each row that specifies the start- and endtime for the partial AUC
calculation. At least the AUC_0-t (\code{auclast}) will be calculated for each
row. Any other parameters specified in the data.frame as column will also be
calculated, e.g. \code{cmax=TRUE}. Currently, only a single row can be supplied
as argument.}

\item{exclude_points}{list of points to exclude for entire NCA analysis,
based on name(s) in the list that should match column name in the input
dataset.
E.g. \code{list("sample_id" = c("12345", "23456"))}. This could similarly be used
to exclude entire subjects, e.g. \verb{list("subject_id = c("103, "105")}}

\item{exclude_subjects}{optional, vector of subjects to exclude from NCA}

\item{exclude_lambda_z}{list of points to exclude but exclusions are only
for the calculation of lambda-z and downstream parameters (halflife, CL, V,
AUCinf etc) but not summarization parameters like Cmax, Tmax, etc.
List with similar structure as \code{exclude_points}.}

\item{include_lambda_z}{list of points to include for the calculation of
lambda-z and downstream parameters (halflife, CL, V, AUCinf etc) but not
summarization parameters like Cmax, Tmax, etc.
List with similar structure as \code{exclude_points}.
Please note that for any subject and interval where
\code{include_lambda_z} are specified, PKNCA will not run the automated
curve-stripping procedure and the \code{exclude_lambda_z} argument (if specified)
will be ignored for that subject and AUC interval.}

\item{units}{optional units specification, passed to \code{PKNCA::PKNCAdata()}.
Either a named list with elements \code{concu}, \code{timeu}, \code{doseu}, and/or
\code{amountu}, which are forwarded to \code{PKNCA::pknca_units_table()} internally;
or a pre-built data frame from \code{PKNCA::pknca_units_table()}. When provided,
a \code{units} attribute containing a per-parameter units table is attached to
the returned data frame.

To automatically simplify derived units (e.g. V in \code{L}, CL in \code{L/h}),
include \code{concu_pref} and/or \code{doseu_pref} in the list — these are forwarded
to \code{PKNCA::pknca_units_table()} and use the \code{units} R package to compute
conversion factors automatically. For example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{units = list(
  concu = "ng/mL", timeu = "h", doseu = "mg",
  concu_pref = "mg/L"   # makes V = mg/(mg/L) = L, CL = L/h
)
}\if{html}{\out{</div>}}}

\item{conversions}{optional manual unit conversion table — a data.frame with
columns \code{PPORRESU} (original unit), \code{PPSTRESU} (preferred unit), and
\code{conversion_factor} (multiply PPORRESU value to obtain PPSTRESU value), as
accepted by \code{PKNCA::pknca_units_table(conversions = ...)}. This is an
alternative to \code{concu_pref}/\code{doseu_pref} that does not require the \code{units}
R package. Only used when \code{units} is a named list (not a pre-built
data.frame). See the \emph{Unit strings and conversions} section for format
details.}

\item{sequence_from}{optional. Argument should specify the column name
that indicates the treatment e.g. for food-effect cross-over trials. It
will then add sequence group and sequence description columns to the
output data.}

\item{add_auctau}{compute AUCtau whenever relevant? TRUE (default) / FALSE.
For multi-dose PK data, this will add the parameter \code{auc_tau} to the NCA
output data as the AUC for each interval. In essence, it will just make a
copy of the AUClast for the interval. See exact logic in function
help page.}

\item{post}{named list of lists, indicating which hard-coded
post-processing steps to include, and options for each step.
Possible post-processing steps include:
\itemize{
\item \code{accumulation}: if NCA calculated for multiple non-overlapping intervals,
will add accumulation ratios for the parameters listed in the \code{parameters}
argument to the NCA output table for each interval.
}

Other options may be added in future. Default is:
\code{list("accumulation" = list("parameters" = c("auclast", "cmax")))}.}

\item{format}{output as table in wide or long format.}

\item{no_dots}{if \code{TRUE} (default) will replace any dots in parameter names
(e.g. \code{aucinf.obs}) with underscores (\code{aucinf_obs}).}

\item{path}{path to file to store output object from PKNCA as RDS (optional)}

\item{check_grouping}{check whether the grouping specified in \code{groups} is
likely to be correct for NCA, and is not creating groups that are too small
for NCA to be able to calculate half-life and other parameters.
See \code{check_nca_grouping()} for details.}

\item{verbose}{verbose output?}

\item{...}{parameters passed to `PKNCA::pk.nca()`` function.}
}
\description{
This function runs an NCA on a pre-parsed dataset. The NCA will be performed
using the PKNCA package. The NCA output object will be saved to an
rds file if \code{path} is specified.
}
\details{
There are four possible types of input datasets that can be used to run the
NCA:
\enumerate{
\item Using a separate data.frame for PK data (\code{data}) and dose data
(\code{dose_data}), with each PK or dosing event a row in the respective datasedt.
Can be used for both single and multi-dose data.
\item Using a single data.frame for PK and dose data (\code{data}), with event type
separated using an evid column (specified in \code{dictionary}). PK data rows
should be set to \code{evid=0}, and doses to \code{evid=1}. This is essentially a
NONMEM-style dataset. Rows with other \code{evid} values will be ignored.
Can be used for both single and multi-dose data, the dosing occasions will
be determined from the dosing events. If an \code{evid} column is present in
\code{data}, it will not attempt to use approach 3.
\item Using a single data.frame (\code{data}) with PK data as rows, and a column for
dose (with colum name specified in \code{dictionary}). A \code{dose_data} data.frame
will be constructed from the dose column. Can only be used for single-dose
data, unless \code{groups} are used to separate the dosing occasions.
\item Using a single data.frame (\code{data}) with PK data as rows, but no dosing
info in the data. NCA can still compute AUC and Cmax etc, but parameters that
require the dose such as CL, V, etc will be returned as NA. Can only be used
for single dose, unless \code{groups} are used to separate the dosing occasions.
}
}
\section{Unit strings and conversions}{


\strong{Unit string format}

The \code{concu}, \code{timeu}, \code{doseu}, and \code{amountu} fields in the \code{units} list are
free-form label strings. PKNCA passes them through as-is to build composite
unit labels for each NCA parameter (e.g. AUC = \code{timeu*concu}, V =
\code{doseu/(concu)}, CL = \code{doseu/(timeu*concu)}). There is no built-in
validation or normalisation, so:
\itemize{
\item \strong{Use consistent, conventional abbreviations} throughout: \code{"mg"} not
\code{"MG"} or \code{"milligram"}, \code{"h"} not \code{"hr"} or \code{"hours"}, \code{"ng/mL"} not
\code{"ng*mL-1"}.
\item \strong{Case is significant}: \code{"mg"} and \code{"MG"} produce different PPORRESU
strings, which matter when matching entries in \code{conversions}.
\item \strong{Concentration units} should be written as \code{"<mass>/<volume>"}, e.g.
\code{"ng/mL"}, \code{"ug/mL"}, \code{"mg/L"}, \code{"nmol/L"}.
\item \strong{Dose units} may include a body-weight denominator, e.g. \code{"mg/kg"}.
}

\strong{Using \code{conversions} (no \code{units} package required)}

The \code{conversions} data.frame maps a raw PPORRESU string to a preferred
display unit with a numeric conversion factor:\tabular{ll}{
   Column \tab Description \cr
   \code{PPORRESU} \tab Exact PPORRESU string as produced by PKNCA (case-sensitive) \cr
   \code{PPSTRESU} \tab Preferred output unit label \cr
   \code{conversion_factor} \tab Multiply the raw value by this to get the preferred value \cr
}


PPORRESU strings for common NCA parameters follow these patterns (where the
unit variables are exactly the strings you passed in \code{units}):\tabular{lll}{
   Parameter type \tab PPORRESU pattern \tab Example \cr
   Concentration (Cmax, Ctrough) \tab \code{concu} \tab \code{"ng/mL"} \cr
   Time (t½, Tmax) \tab \code{timeu} \tab \code{"h"} \cr
   AUC \tab \code{timeu*concu} \tab \code{"h*ng/mL"} \cr
   AUMC \tab \code{timeu^2*concu} \tab \code{"h^2*ng/mL"} \cr
   Volume (Vz, Vss) \tab \code{doseu/(concu)} \tab \code{"mg/(ng/mL)"} \cr
   Clearance (CL) \tab \code{doseu/(timeu*concu)} \tab \code{"mg/(h*ng/mL)"} \cr
}


Example — converting V and CL to L / L/h for \code{concu = "ng/mL"},
\code{doseu = "mg"}, \code{timeu = "h"} (conversion factor = 1000 because
1 mg/(ng/mL) = 1000 L):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{run_nca(data,
  units = list(concu = "ng/mL", timeu = "h", doseu = "mg"),
  conversions = data.frame(
    PPORRESU          = c("mg/(ng/mL)",   "mg/(h*ng/mL)"),
    PPSTRESU          = c("L",            "L/h"),
    conversion_factor = c(1000,           1000)
  )
)
}\if{html}{\out{</div>}}

\strong{Using \code{concu_pref} / \code{doseu_pref} (requires the \code{units} R package)}

As an alternative, PKNCA can derive conversion factors automatically via the
\code{units} package. Pass \code{concu_pref} (and/or \code{doseu_pref}, \code{timeu_pref},
\code{amountu_pref}) inside the \code{units} list. Unit strings for \verb{_pref} fields
must be recognised by the \code{units} package (standard SI abbreviations).

The preferred concentration unit controls the derived units. To obtain V in
L and CL in L/h, choose a \code{concu_pref} whose mass unit matches \code{doseu}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# concu = "ng/mL", doseu = "mg" -> set concu_pref = "mg/L"
# V = mg / (mg/L) = L, CL = mg / (h * mg/L) = L/h
run_nca(data,
  units = list(concu = "ng/mL", timeu = "h", doseu = "mg",
               concu_pref = "mg/L")
)
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
run_nca(
  data = data,
  path = "./outputs/nca.rds"
)
}
}
